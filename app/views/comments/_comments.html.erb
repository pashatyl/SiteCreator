<% if can? :create, Comment %>
    <%= render partial: "comments/form", locals: { site: site } %>
<% end %>

<ul>
  <% site.comments.persisted.each do |comment| %>
      <li>
        <h3><i class="fa fa-comment"></i> <%= comment.content %></h3>
        <p><%= comment.user.name %>,
          <small> <%= l comment.created_at, format: :long %> </small>
        </p>
      </li>
  <% end %>
</ul>
